name: codeBlogFlutter
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get_it: ^3.0.3
  cupertino_icons: ^0.1.2
  http: ^0.12.0+2
  dio: ^3.0.3
  flutter_speed_dial: ^1.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  
flutter:

  uses-material-design: true

